<UserControl x:Class="staymanager_pj.Views.Shared.TopBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="56">
    <Border Background="White" BorderBrush="#D9D9D9" BorderThickness="0,0,0,1" Padding="16,10">
        <DockPanel>
            <TextBlock Text="StayManager" FontSize="22" FontWeight="Bold" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnHome" Content="Trang chủ" Width="90" Margin="0,0,8,0"/>
                <Button x:Name="btnRoomList" Content="Phòng" Width="80" Margin="0,0,8,0"/>
                <Button x:Name="btnLogin" Content="Đăng nhập" Width="90" Margin="0,0,8,0"/>
                <Button x:Name="btnRegister" Content="Đăng ký" Width="90"/>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>


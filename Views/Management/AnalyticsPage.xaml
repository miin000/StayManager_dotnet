<Window x:Class="staymanager_pj.Views.Management.AnalyticsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:staymanager_pj.Views.Shared"
        Title="Thống kê" Height="900" Width="1200" WindowStartupLocation="CenterScreen" Background="#F5F5F5">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:Sidebar Grid.Column="0" Loaded="Sidebar_Loaded"/>
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Thống kê tổng quan" FontSize="24" FontWeight="Bold" Margin="10"/>
                <ItemsControl x:Name="icDashboardStats" Margin="10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="6" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#666666"/>
                                    <TextBlock Text="{Binding Value}" FontSize="26" FontWeight="Bold" Margin="0,8,0,4"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#888888" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="Doanh thu" FontSize="20" FontWeight="Bold" Margin="10"/>
                <ItemsControl x:Name="icRevenueStats" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10" Padding="16">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="1" Text="{Binding RevenueText}" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="2" Text="{Binding InvoiceCountText}" FontSize="14" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="Tình trạng phòng" FontSize="20" FontWeight="Bold" Margin="10"/>
                <ItemsControl x:Name="icRoomStats" Margin="10">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="0,0,0,10" Padding="16">
                                <StackPanel>
                                    <DockPanel>
                                        <TextBlock Text="{Binding RoomType}" FontSize="15" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding OccupancyRateText}" DockPanel.Dock="Right" FontSize="14" FontWeight="SemiBold" Foreground="#2F6FED"/>
                                    </DockPanel>
                                    <TextBlock Text="{Binding Summary}" FontSize="13" Margin="0,8,0,0" Foreground="#555555"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="Vật tư" FontSize="20" FontWeight="Bold" Margin="10"/>
                <ItemsControl x:Name="icInventoryStats" Margin="10">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="6" Padding="16">
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="13" Foreground="#666666"/>
                                    <TextBlock Text="{Binding Value}" FontSize="24" FontWeight="Bold" Margin="0,8,0,4"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="#888888" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
